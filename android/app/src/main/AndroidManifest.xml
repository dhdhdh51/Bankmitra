<?xml version="1.0" encoding="utf-8"?>
<!--
    LRMS - Loan Recovery Management System (BC field app)

    Permission notes for reviewers:
      INTERNET / ACCESS_NETWORK_STATE - talk to the /api/v1 backend
      ACCESS_FINE_LOCATION            - a visit is only valid with a GPS fix
      ACCESS_COARSE_LOCATION          - fallback while the fine fix is acquired
      CAMERA                          - watermarked visit photos and selfies
      POST_NOTIFICATIONS              - Android 13+ runtime notification consent
      VIBRATE                         - queue-sync feedback

    There is deliberately NO background-location permission: GPS pings are sent
    by a WorkManager job while the app is in use, not by silent 24/7 tracking.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- A GPS-less device cannot do the job, but we do not want the app to be
         uninstallable on tablets used by branch managers, so required=false. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".LrmsApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lrms"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Lrms.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LoginActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.RegisterActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.DashboardActivity" android:exported="false" />
        <activity android:name=".ui.CustomerListActivity" android:exported="false" />
        <activity android:name=".ui.CustomerDetailActivity" android:exported="false" />
        <activity android:name=".ui.VisitFormActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.RecoveryActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.AttendanceActivity" android:exported="false" />
        <activity android:name=".ui.SyncQueueActivity" android:exported="false" />
        <activity android:name=".ui.SettingsActivity" android:exported="false" />

        <!-- Camera hand-off. Without a FileProvider, ACTION_IMAGE_CAPTURE
             throws FileUriExposedException on Android 7+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- The Maps API key is delivered by the server at runtime, so this is
             an intentionally empty placeholder. The MapView is only used for
             read-only display and degrades to a coordinate list without it. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key_placeholder" />

        <!-- OPTIONAL FIREBASE: uncomment together with the dependencies in
             app/build.gradle.kts and google-services.json.
        <service
            android:name=".push.LrmsMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

    </application>
</manifest>
