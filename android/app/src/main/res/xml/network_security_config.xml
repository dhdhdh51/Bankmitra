<?xml version="1.0" encoding="utf-8"?>
<!--
    HTTPS is required for every real server.

    The single exception below allows plain HTTP to localhost / 10.0.2.2 so a
    developer can point the app at `php -S` or an emulator host during testing.
    A production deployment must use HTTPS (cPanel AutoSSL is free), and the
    app refuses cleartext to any other host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- The emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
